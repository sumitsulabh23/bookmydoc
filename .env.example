# Backend env vars — copy this to backend/.env and fill in values

# MongoDB connection string
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.urdee6m.mongodb.net/meditrack?retryWrites=true&w=majority

# JWT
JWT_SECRET=generate_a_strong_random_secret_here
JWT_EXPIRES_IN=7d

# Port (leave unset on Render/Railway — they inject PORT automatically)
PORT=5001

# Comma-separated list of allowed frontend origins (no trailing slash)
# Example: https://bookmydoc.vercel.app,https://www.bookmydoc.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174
